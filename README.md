# aws-serverless-crud-api-dynamodb
This repo to keep the artifacts that are created as part of building the serverless architecture that API gateway accepts the HTTPS request (Get/Post/Delete/Update) that invokes the lambda function that connects to DynamoDB and store the data.
